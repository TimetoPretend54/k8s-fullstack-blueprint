{{- if .Values.secrets.backend.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "fullstack.fullname" . }}-backend-secret
  namespace: {{ .Values.namespace | default "default" }}
  labels:
    {{- include "fullstack.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets.backend.data }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
